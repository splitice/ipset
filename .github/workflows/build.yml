on:
  push:
  workflow_dispatch:
        
name: "build-ipset"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: AutoModality/action-clean@v1
      - uses: actions/checkout@v2
      - name: "prepare"
        run: |
          sudo apt-get install -y libltdl-dev pkgconf libmnl-dev
          ./autogen.sh
          ./configure
      - name: "make"
        run: |
          make
          make modules